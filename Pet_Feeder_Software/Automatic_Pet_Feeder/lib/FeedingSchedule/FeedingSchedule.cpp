#include <FeedingSchedule.h>

BTtime_fs feedschedule[6];

void FeedingSchedule_Pet(void)
{
  while(1){
    //   if (xSemaphoreTake(mutexData, 0) == pdTRUE)
    // {
      

    // }
  vTaskDelay(100/portTICK_PERIOD_MS);
  }
}